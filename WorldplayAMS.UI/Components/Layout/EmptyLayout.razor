@inherits LayoutComponentBase

<div class="empty-layout">
    @Body
</div>

<style>
    .empty-layout {
        min-height: 100vh;
        background-color: var(--surface-bg);
        background-image: 
            linear-gradient(45deg, rgba(0,240,255,0.03) 0%, transparent 40%),
            linear-gradient(-45deg, rgba(255,0,170,0.03) 0%, transparent 40%);
        display: flex;
        align-items: flex-start;
        justify-content: center;
        padding: 2rem 1rem;
    }
</style>
